# windows-vm-provisioner
Pipeline completo para provisionamento automatizado de VMs Windows via QEMU/KVM no Linux. Cria uma ISO WinPE customizada com suporte UEFI, sobe a VM com hardware emulado e realiza a instalação desassistida do Windows com drivers VirtIO e particionamento automático.
